import Navbar from './Navbar';

/** The **Navbar** component renderize the main navbar of the application. */
export default {
  title: 'Patterns/Navbar/Navbar',
  component: Navbar,
  tags: ['autodocs'],
  argTypes: {
  },
}

/**This is the default version of **Navbar** component:*/
export const Default = {
  args: {},
}