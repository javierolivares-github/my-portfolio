import Hero from './Hero';

/** The **Hero** component renderize the Hero section of the application. */
export default {
  title: 'Patterns/Hero/Hero',
  component: Hero,
  tags: ['autodocs'],
  argTypes: {
  },
}

/**This is the default version of **Hero** component:*/
export const Default = {
  args: {},
}