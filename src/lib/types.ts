export type TextProps = {
  children: string;
  color: string;
}

export type IconProps = {
  fillColor?: "text-neutral-900";
  width: string;
  height:string;
  smWidth?: string;
  smHeight?: string;
  isFilled?: boolean;
}